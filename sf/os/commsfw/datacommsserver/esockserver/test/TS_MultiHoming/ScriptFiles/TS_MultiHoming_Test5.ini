[ShowConnections]
# single IPv4 interface address on each interface
ExpectedNumOfIfs=1
OOMTest=FALSE

[Delay]
OOMTest=FALSE
# wait 10 seconds for connections to close - 
# this depends on the value in the cfg file, which is set to 30
DelayInMs=10000
